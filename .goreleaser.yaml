builds:
  - main: ./cmd/
#  - goos:
#      - linux
#      - darwin
#  - goarch:
#      - amd64
#      - arm64

release:
  github:
    owner: fastid
    name: tmp

  target_commitish: '{{ .Commit }}'
  prerelease: auto
  mode: append

  name_template: "{{.ProjectName}} v{{.Version}}"

